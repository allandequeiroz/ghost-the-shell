version: '3.2'

services:

  # Ghost server    
  ghost:
    image: allandequeiroz/ghost-the-shell
    restart: always  
    ports:
      - mode: host
        target: 2368
        published: 80
    volumes:
      - $PWD/data/blog:/var/www/ghost/content/data/
